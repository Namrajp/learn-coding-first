---
import { getCollection } from "astro:content";
import Layout from "@layouts/Layout.astro";
let posts = await getCollection("posts");

const tags: string[] = [];
posts.map((post) => {
  post.data.tags.map((tag) => {
    if (!tags.includes(tag)) tags.push(tag);
  });
});

posts = posts
  .sort(
    (a, b) => new Date(b.data.date).valueOf() - new Date(a.data.date).valueOf()
  )
  .slice(0, 12);
---

<Layout title="Homepage">
  <div class="w-full text-left pt-[0.5rem]">
    <div class="fixed top-0 left-0 right-0 z-50 flex items-center justify-center bg-[hsl(21,62%,45%)] px-8 pt-3 pb-[1.25rem] text-center text-white shadow-sm">
      <p class="text-base font-semibold leading-tight">
        Coming Soon: AI Workshop and new content releasing every week.
      </p>
    </div>

    <div class="mt-0 grid gap-6 md:grid-cols-[30%_70%] md:pl-[10rem]">
      <aside class="md:pr-10 px-4 md:px-6 max-h-[calc(100vh-140px)] overflow-y-auto">
        <h2 class="text-5xl font-black tracking-tight text-black mb-3 leading-tight border-b-[15px] border-black pb-3 inline-block">
          <span class="block">Learn CODING</span>
          <span class="block pl-6">First</span>
        </h2>

        <ol class="my-4 space-y-3 list-decimal list-inside text-slate-700">
          {
            tags.map((tag) => (
              <li>
                <a
                  href={`/tag/${tag}`}
                  class="capitalize no-underline hover:text-[hsl(21,62%,45%)]"
                >
                  {tag}
                </a>
              </li>
            ))
          }
        </ol>
      </aside>

      <section>
        <h1 class="text-5xl font-extrabold tracking-tight text-slate-900 mb-4">
          Fundamentals of programming and AI
        </h1>
        <p class="my-4 text-xl font-semibold">Latest posts</p>
        <ul class="space-y-2">
          {
            posts.map((post) => (
              <li>
                <a href={`/blog/${post.id}`} class="no-underline hover:underline-offset-4 hover:text-[hsl(21,62%,45%)]">
                  {post.data.title}
                </a>
                <span class="ml-3 text-sm text-slate-500">({post.data.date.toLocaleDateString()})</span>
              </li>
            ))
          }
        </ul>

        <p class="py-4">
          <a href={"/blog/page/1"}> See all posts ▶︎ </a>
        </p>
      </section>
    </div>
  </div>
</Layout>
